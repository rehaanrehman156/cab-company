apiVersion: v1
kind: Service
metadata:
  name: cab-backend
  namespace: cab-company
spec:
  selector:
    app: cab-backend
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
